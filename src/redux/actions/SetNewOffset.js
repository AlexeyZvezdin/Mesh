import axios from '../../axios';
// action types is really a stupid thing. If only it's not used in multiple files
// ok I'll create one more there
import { SET_DRIVERS_OFFSET } from '../actionTypes';
import fetchDrivers from './index';

// Лол новую писать не обязательно
